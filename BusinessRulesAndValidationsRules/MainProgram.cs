using Newtonsoft.Json.Serialization;
using Newtonsoft.Json;

namespace BusinessRulesAndValidationsRules
{
    public class MainProgram
    {
        public static void Main()
        {
            //var produto = new Product();

            //var state = new ValidationModelState().Valid(produto);
            //var stateVen = new ValidationModelState().Valid(new Vendedores());

            //var camelSettings = new JsonSerializerSettings { ContractResolver = new CamelCasePropertyNamesContractResolver() };
            //var json = Newtonsoft.Json.JsonConvert.SerializeObject(state, camelSettings);

            //var fluent = new ProductValidator();
            //var valids = fluent.Validate(produto);
            //foreach (var item in valids.Errors)
            //{
            //}

            //var jsonFluent = Newtonsoft.Json.JsonConvert.SerializeObject(valids, camelSettings);
            //var response = new ResponseModel();
        }
    }
}